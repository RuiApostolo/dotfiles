- defaults:
    link:
        create: true
        glob: true

- clean: ['~']

- link:
    ~/.profile:
    ~/.bashrc:
    ~/.bash_logout:
    ~/.vim/:
        path: vim/*

- shell:
    # should keep submodules on proper versions
    - [git submodule update --init --recursive, Installing submodules]

